已掌握

import pandas as pd
# 读取 CSV 文件（假设文件在当前目录）
df = pd.read_csv()
# 读取 Excel 文件
df = pd.read_excel('', sheet_name='Sheet1')

查看数据基本信息
方法	            说明
df.head(n)	显示前 n 行数据（默认 5 行）
df.tail(n)	显示后 n 行数据（默认 5 行）
df.info()	显示数据的信息，包括列名、数据类型、缺失值等
df.describe()	显示数值列的基本统计信息（均值、方差、最大值、最小值等）
df.shape	返回 (行数, 列数)
df.columns	查看所有列名
df.index	查看行索引

索引与筛选
.loc 使用行标签（索引名）和列名来选择数据，包含切片的两端
.iloc 使用整数位置（从 0 开始）来选择数据，类似于列表索引，切片不包含末尾。
布尔索引
filtered = df[(df['数学'] > 85) & (df['语文'] > 85)]

添加、修改和删除列
df['newname']=df[''] + df[''] + int  or ['手动添加1或者行数的值']
df = df.drop('name', axis=1) # axis=1 表示按列删除

处理缺失数据
# 检查每个元素是否为缺失
print(df_missing.isna())
# 删除全部包含缺失的行
df_clean = df_missing.dropna(how='all',axis=1)  # axis=1 删除纵向列
# 用常数填充
df_filled = df_missing.fillna(0)

数据排序
df_sorted = df.sort_values(by=['班级', '数学'], ascending=[True, False]) # False 降序,True 升序

分组聚合
result = df.groupby('班级', as_index=False).agg({
    '数学': 'mean',
    '语文': 'max',
    '英语': 'min'
}) # as_index=False 重置索引
grouped = df.groupby(['班级', '性别'])['数学'] # 多列分组

merge —— 数据库风格的连接
result = pd.merge(students, scores, on='学号', how='inner') # left 左连接 right 右连接 outer 外连接

保存结果到 Excel 文件
with pd.ExcelWriter("名称.xlsx",engine="xlsxwriter") as writer:
    内容.to_excel(writer, sheet_name = "Sheet1", index = False,header=False) # index = False(去行表头),header=False(去列表头)

未掌握
无

后续计划
巩固今日所学，复习前面内容