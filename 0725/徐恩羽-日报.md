已掌握

对Pandas和NumPy掌握更加熟练

包括但不限于

NumPy

从数组中取数据 —— 索引和切片

向量化：
指对数组的所有元素同时执行相同的操作，不需要显式编写循环。NumPy 在底层使用 C 语言循环，速度极快

快捷创建特殊数组
通用函数

重塑
改变数组的形状，不改变数据本身。

Pandas
布尔索引
loc 使用行标签（索引名）和列名来选择数据，包含切片的两端。
iloc 使用整数位置（从 0 开始）来选择数据，类似于列表索引，切片不包含末尾。
添加、修改和删除列
处理重复数据
分组聚合
grouped = df.groupby('分组名')['数据名'].mean()
merge —— 数据库风格的连接
result = pd.merge(students, scores, on='学号', how='inner') # left 左连接 right 右连接 outer 外连接

未掌握
FastAPI上传文件和下载文件未能完全掌握

后续计划
多练习未掌握部分，预习后续部分