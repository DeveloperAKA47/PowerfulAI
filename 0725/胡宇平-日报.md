## 已掌握
json.dump

```py
# 简洁的把json数据以文件形式返回，只需在返回的response里加上响应头
 return JSONResponse(
            content=books_db,
            headers={
                "Content-Disposition": 'attachment; filename="books.json"'
            },
        )
```
## 未掌握
无
## 后续计划
异常处理  异步编程